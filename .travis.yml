sudo: false
cache: bundler

language: ruby
rvm:
  - 2.3.1

services: postgres

before_script:
  - bundle exec rake db:create
  - bundle exec rake db:migrate

script:
  - bundle exec rake spec

notifications:
  recipients:
    - <your email>
